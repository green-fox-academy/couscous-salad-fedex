# couscous-salad-fedex

1) How to launch the backend application:
- in the /backend directory in the terminal you can launch the application with the following command:

$ java -jar fedex-0.0.1-SNAPSHOT.jar

